# DB GUI 1.0.0 — 产品功能与 Features 清单

> 版本：`1.0.0`  
> 产品：DB GUI（Tauri 2 + SvelteKit 桌面数据库客户端）  
> 引擎：MySQL / PostgreSQL  
> 状态说明：**已完成** / **部分完成** / **未实现**

---

## 1. 连接与会话

| 功能 | 状态 | 说明 |
|------|------|------|
| 保存连接配置 | 已完成 | 持久化到应用数据目录 `connections.json` |
| 新建 / 编辑 / 删除连接 | 已完成 | 连接对话框 + 删除确认 |
| 引擎选择（MySQL / PostgreSQL） | 已完成 | 按引擎预设端口、用户名、默认库 |
| 连接 URL 粘贴解析 | 已完成 | 支持 `mysql://`、`mariadb://`、`postgres(ql)://`、`pgsql://` |
| 可选保存密码 | 已完成 | 未勾选时不持久化密码 |
| 测试连接 | 已完成 | |
| 连接 / 断开 | 已完成 | 工具栏与对象树菜单 |
| 认证失败密码提示 | 已完成 | `PasswordDialog` |
| 最大并发会话 | 已完成 | 上限 5；超出时淘汰最早会话 |
| SSL / TLS | 未实现 | PostgreSQL 使用 `NoTls`；MySQL 无 SSL 配置 |
| SSH 隧道 | 未实现 | |
| 连接分组 / 收藏 | 未实现 | 目前为扁平列表 |

---

## 2. 对象树与浏览

| 功能 | 状态 | 说明 |
|------|------|------|
| 连接节点（状态角标、展开） | 已完成 | 双击连接 |
| 数据库 / Schema 节点 | 已完成 | PostgreSQL 显示为 Schema |
| 文件夹：表 / 视图 / 索引 / 触发器 / 函数 | 已完成 | 按需懒加载 |
| 对象打开 | 已完成 | 表/视图→工作区；例程/触发器→DDL；索引→列表页 |
| 右键上下文菜单 | 已完成 | 见下方菜单明细 |
| 「设计表」菜单项 | 部分完成 | i18n 与处理逻辑存在，对象树未暴露入口 |
| 对象列表筛选 | 已完成 | 按名称客户端过滤 |

**对象树右键菜单**

- 未连接：连接、编辑、删除、新建连接
- 已连接：新建查询、创建库/Schema、刷新、断开、编辑、删除
- 数据库/Schema：新建查询、运行 SQL 文件、创建、导出 Dump、删除（非系统）、刷新
- 文件夹：刷新
- 表/视图：打开数据、查看 DDL、新建查询、刷新
- 其他对象：查看 DDL、刷新

---

## 3. 表 / 视图工作区

| 功能 | 状态 | 说明 |
|------|------|------|
| 子页签：数据 / 结构 / DDL | 已完成 | 视图禁止行变更 |
| 数据分页预览 | 已完成 | 上/下一页，页大小 100 |
| 估算行数展示 | 部分完成 | 来自元数据估算，非实时 `COUNT(*)` |
| 精确行数 API | 部分完成 | 后端 `table_row_count` 已实现，前端未调用 |
| 表格视图 / 表单视图 | 已完成 | |
| 插入 / 编辑 / 删除行 | 已完成 | 对话框编辑；按主键 WHERE；无主键则全列匹配 |
| 复制行为 INSERT SQL | 已完成 | 行右键菜单 |
| 单元格内联编辑 | 未实现 | 网格只读，通过对话框编辑 |
| 结构网格 | 已完成 | 名称、类型、可空、键、默认值、额外、注释 |
| 添加 / 编辑 / 删除列 | 已完成 | 方言感知 ALTER；PG 多语句 + `COMMENT ON` |
| DDL 查看 | 已完成 | MySQL `SHOW CREATE`；PostgreSQL 重建 DDL |

---

## 4. 数据网格与结果

| 功能 | 状态 | 说明 |
|------|------|------|
| 虚拟滚动 | 已完成 | 超过 80 行窗口化渲染 |
| NULL 显示 | 已完成 | |
| 长单元格详情抽屉 | 已完成 | 可复制到剪贴板 |
| 表单逐条浏览 | 已完成 | 上一条 / 下一条 |
| 结果行数上限 | 已完成 | 后端单次最多返回 5,000 行 |

---

## 5. SQL 查询编辑器

| 功能 | 状态 | 说明 |
|------|------|------|
| CodeMirror SQL 编辑器 | 已完成 | 历史、折叠、当前行；`⌘/Ctrl+Enter` 执行 |
| 方言高亮（MySQL / PostgreSQL） | 已完成 | Oracle 仅在辅助层，无引擎 |
| 运行 / 格式化 / Explain | 已完成 | 快捷键：Mod-Enter、Mod-Shift-F、Mod-Shift-E |
| Schema 上下文 | 已完成 | MySQL `USE`；PostgreSQL `search_path` |
| 结果区 + 执行日志 | 已完成 | |
| 打开并自动运行 SQL 文件 | 已完成 | 文件上限 8 MB |
| 多语句 / 多结果集 UX | 部分完成 | 引擎可执行；UI 主要展示单一结果集 |
| 查询历史 / 已保存脚本 | 未实现 | |

---

## 6. 数据库 / Schema 运维

| 功能 | 状态 | 说明 |
|------|------|------|
| 创建数据库（MySQL） | 已完成 | 可选字符集 / 排序规则 |
| 创建 Schema（PostgreSQL） | 已完成 | 隐藏字符集 UI |
| 删除数据库 / Schema | 已完成 | 系统对象受保护；PG 使用 `DROP SCHEMA … CASCADE` |
| Dump 导出 `.sql` | 已完成 | 可选含数据；单表最多 50,000 行 |
| 运行 SQL 文件 | 已完成 | |
| 还原向导（Import Dump） | 未实现 | 仅「运行 SQL 文件」 |
| 可视化创建/删除/重命名表等对象 | 未实现 | 需手写 SQL |

---

## 7. 界面与体验

| 功能 | 状态 | 说明 |
|------|------|------|
| 应用壳（工具栏 + 树 + 页签 + 状态栏） | 已完成 | |
| 可调整左右分栏 | 已完成 | |
| 查询编辑器上下分栏 | 已完成 | |
| 欢迎页 | 已完成 | |
| 全局快捷键 | 已完成 | `F5` 刷新；`⌘/Ctrl+Shift+N` 新建连接 |
| 中英文切换 | 已完成 | `en` / `zh`，`localStorage` 持久化 |
| 主题切换 | 未实现 | |
| 工作区跨重启持久化 | 未实现 | 页签/展开状态仅内存 |

**工具栏：** 新建连接 · 连接 · 断开 · 创建库/Schema · 新建查询 · 刷新 · 连接状态 · EN/中文

---

## 8. 引擎能力矩阵

| 能力 | MySQL | PostgreSQL |
|------|-------|------------|
| 连接 / 测试 / 连接池 | 已完成 | 已完成 |
| 列出库 / Schema | 已完成 | 已完成 |
| 创建 / 删除 | 已完成 | 已完成 |
| 字符集目录 | 已完成 | 部分完成（空目录） |
| 表 / 视图 / 索引 / 触发器 / 例程 | 已完成 | 已完成 |
| 列信息 | 已完成 | 已完成 |
| DDL | 已完成 | 已完成 |
| 独立索引 DDL | 部分完成（显式报错） | 已完成 |
| 预览 / 执行 / 行数 | 已完成 | 已完成 |
| Dump | 已完成 | 已完成 |
| MariaDB | 部分完成（按 MySQL 处理） | — |
| Oracle | 未实现（仅高亮/格式化辅助） | — |

---

## 9. 后端 Tauri Commands（22）

**连接：** `list_connections` · `upsert_connection` · `delete_connection` · `test_connection` · `connect_session` · `disconnect_session`

**结构：** `list_databases` · `create_database` · `drop_database` · `dump_database` · `list_charset_catalog` · `list_tables` · `list_views` · `list_indexes` · `list_triggers` · `list_routines` · `get_columns` · `get_ddl`

**查询：** `preview_table` · `table_row_count` · `execute_sql`

---

## 10. 1.0.0 范围总结

### 已交付核心能力

- MySQL + PostgreSQL 双引擎桌面客户端
- Navicat 风格对象树、数据网格、结构编辑、DDL 查看、SQL 编辑器
- 连接管理、库/Schema 创建与删除、Dump 导出、运行 SQL 文件
- 行级 CRUD、列级 ALTER、Explain、SQL 格式化
- 中英文界面

### 已知限制

- 无 SSL / SSH
- Dump 单表 50k 行、查询结果 5k 行上限
- 无完整对象向导（建表/索引等）、无还原向导、无 CSV 导入导出
- 部分 API/菜单项未完全接入 UI（精确行数、设计表入口、MySQL 索引 DDL）

### 后续候选（未纳入 1.0.0）

- SSL/TLS 与 SSH 隧道
- Oracle 及其他引擎
- 内联单元格编辑、CSV 导入导出、还原向导
- 可视化建表/索引/触发器向导
- 查询历史、已保存脚本、连接分组、主题
